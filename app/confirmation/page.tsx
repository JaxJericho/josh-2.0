import { Confirmation } from "@/components/Confirmation";

export const metadata = {
  title: "You're on the list — JOSH",
  description: "Registration confirmed.",
};

export default function ConfirmationPage() {
  return <Confirmation />;
}
