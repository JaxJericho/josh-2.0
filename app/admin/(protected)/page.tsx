export default function AdminHomePage() {
  return (
    <section>
      <h1 style={{ marginTop: 0 }}>Admin Dashboard Foundation</h1>
      <p>
        Ticket 12.1 is active: secure admin auth, RBAC, session controls, and immutable audit logging are now
        enforced for admin routes.
      </p>
    </section>
  );
}
