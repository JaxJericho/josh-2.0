import { Login } from "@/components/Login";

export const metadata = {
  title: "Log in — JOSH",
  description: "Log in to your JOSH account.",
};

export default function LoginPage() {
  return <Login />;
}
