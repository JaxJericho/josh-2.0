# Ticket 21.2 — Matching Job: Remove one_to_one And Update Default Mode (S)

## Role

Principal Systems Architect & Full-Stack Engineer (migration prep, runtime correctness)

## Phase

Phase 21 — Matching System Updates, Destructive Migrations, And Legacy Removal

## Complexity

Small

## What This Ticket Builds

Removes all `one_to_one` references from the matching job so enum recreation (Ticket 21.5) can safely remove `one_to_one` from `match_mode`.

## Scope Locks (Anti-Scope-Creep)

### Allowed Change Surface

- `scripts/run-matching-job.mjs`
- Matching-job tests only (CLI parsing/default behavior)

### Forbidden

- No schema changes (no migrations)
- No scoring changes
- No refactors outside mode parsing/defaults + mode writes

### No New Concepts Rule

No new modes, flags, enums, or abstractions.

### Production Ready Definition

- Default mode is `linkup`
- `--mode one_to_one` fails before any DB writes
- Tests cover both behaviors
- `pnpm lint`, `pnpm typecheck`, `pnpm build` pass; `pnpm test` passes except known baseline guardrail (if still present)

## Requirements (Exact)

In `scripts/run-matching-job.mjs`:

- Remove `one_to_one` from `RUN_MODES` set (**line 10**)
- Remove `one_to_one` as a valid `--mode` argument (**line 18**)
- Change the default mode (**line 374**) from `one_to_one` to `linkup`
- Remove any `mode = 'one_to_one'` writes in `match_runs` and `match_candidates`
- Verify no other application code writes `mode = 'one_to_one'` (grep + fix/block)

## Deliverables

- Updated `scripts/run-matching-job.mjs`
- Tests:
  - passing `--mode one_to_one` throws before DB writes
  - default run mode is `linkup`

## Verification Checklist

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`

## Completion Criteria

- No runtime path accepts or writes `one_to_one`
- Tests prove behavior

## Workflow Requirements (Branch + PR Discipline)

- Spec PR:
  - Branch: `spec/21.2-remove-one-to-one`
  - Docs-only changes under `docs/specs/...`
- Implementation PR:
  - Branch: `ticket/21.2-remove-one-to-one`
  - No spec file changes
- Codex must:
  - Create/checkout `ticket/...`
  - `git add -A`
  - Commit
  - Push (so PR diff exists)
